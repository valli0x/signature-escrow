# Override defaults used by docker-compose.yml.
# Copy to docker/.env or export in your shell before `docker compose up`.

JWT_SECRET=change-me-in-production

# Storage encryption (leave empty to disable)
STORAGE_PASS=
STORAGE_PASS_A=
STORAGE_PASS_B=

# Blockchain
ETHEREUM_RPC=
BLOCKCYPHER_TOKEN=
